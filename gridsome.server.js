module.exports = function (api, options) {

    api.loadSource(actions => {
        const posts = actions.getCollection("Post");

        // posts.addNode({
        //     meta:{
                
        //     }
        // })
    })
}